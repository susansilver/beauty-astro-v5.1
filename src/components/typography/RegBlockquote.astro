---
interface Props {
  cite?: string;
  show: boolean;
}

const { cite, show } = Astro.props;
---

<blockquote>
  <p><slot /> {show === true && <span class="attribution">- {cite}</span>}</p>
</blockquote>
